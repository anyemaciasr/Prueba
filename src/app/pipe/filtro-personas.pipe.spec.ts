import { FiltroPersonasPipe } from './filtro-personas.pipe';

describe('FiltroPersonasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPersonasPipe();
    expect(pipe).toBeTruthy();
  });
});
